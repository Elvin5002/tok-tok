import 'package:flutter/material.dart';

class AppPaddings {
  static const a8 = EdgeInsets.all(8);
  static const a10 = EdgeInsets.all(10);
  static const a16 = EdgeInsets.all(16);
  static const a24 = EdgeInsets.all(24);
  static const h24 = EdgeInsets.symmetric(horizontal: 24);
  static const h8 = EdgeInsets.symmetric(horizontal: 8);
  static const h10 = EdgeInsets.symmetric(horizontal: 10);
  static const a30 = EdgeInsets.all(30);
  static const h30b30 = EdgeInsets.only(left: 30, right: 30, bottom: 30);
  static const t12 = EdgeInsets.only(top: 12);
  static const v2 = EdgeInsets.symmetric(vertical: 2);
}
