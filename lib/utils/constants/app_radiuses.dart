import 'package:flutter/material.dart';

class AppRadiuses {
  AppRadiuses._();

  static const a6 = BorderRadius.all(Radius.circular(6));
  static const a8 = BorderRadius.all(Radius.circular(8));
  static const a10 = BorderRadius.all(Radius.circular(10));
  static const a20 = BorderRadius.all(Radius.circular(20));
  static const a80 = BorderRadius.all(Radius.circular(80));
  static const a60 = BorderRadius.all(Radius.circular(60));
}
